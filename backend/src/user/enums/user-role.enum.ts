/**
 * User Role Enum
 * Defines the three types of users in the courier management system
 */
export enum UserRole {
    ADMIN = 'admin',
    DELIVERY_AGENT = 'delivery_agent',
    CUSTOMER = 'customer',
}
