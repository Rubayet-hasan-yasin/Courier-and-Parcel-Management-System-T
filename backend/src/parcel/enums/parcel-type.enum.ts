/**
 * Parcel Type Enum
 * Categorizes the type of parcel being shipped
 */
export enum ParcelType {
    DOCUMENT = 'document',
    PACKAGE = 'package',
    FRAGILE = 'fragile',
    ELECTRONICS = 'electronics',
    FOOD = 'food',
    OTHER = 'other',
}
